apiVersion: cloud.google.com/v1
kind: BackendConfig

metadata:
  name: product-service-backendconfig
  namespace: ecommerce

spec:
  healthCheck:
    type: HTTP
    requestPath: /health
    port: 5000